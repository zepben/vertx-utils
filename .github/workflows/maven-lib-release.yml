name: Maven Library Release

on: workflow_dispatch

jobs:
  run:
    uses: zepben/.github/.github/workflows/maven-lib-release.yml@main
    with:
      private: false
      java-major-version: "17"
    secrets: inherit
