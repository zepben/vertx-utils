name: Maven Build

on:
  pull_request:
  workflow_dispatch:

jobs:
  run:
    uses: zepben/.github/.github/workflows/maven-build-oss.yml@main
    secrets: inherit
    with:
      java-major-version: "17"

